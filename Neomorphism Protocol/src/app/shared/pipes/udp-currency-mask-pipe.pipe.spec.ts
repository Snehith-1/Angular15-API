import { UdpCurrencyMaskPipePipe } from './udp-currency-mask-pipe.pipe';

describe('UdpCurrencyMaskPipePipe', () => {
  it('create an instance', () => {
    const pipe = new UdpCurrencyMaskPipePipe();
    expect(pipe).toBeTruthy();
  });
});
